# engine-core

Pure, deterministic simulation. No DOM, no fetch, no vendor SDKs. Takes data + inputs, returns new World state + events. Defines the Renderer / Narrative / AudioOut ports. Public API in src/index.ts only.
