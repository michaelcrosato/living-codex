// The ONLY package that may import pixi.js. Implements engine-core's Renderer port.
// Implemented in T-10 (renderer interface + Pixi vector implementation).
export const RENDER_PIXI_PENDING = "T-10" as const;
