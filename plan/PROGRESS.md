# /plan/PROGRESS.md — Execution tracker

Single source of truth for *what's been done*. Update at loop steps 4 (mark `In progress`) and 8
(record outcome). Keep the table honest — `Done` means **DoD met** (see [AGENTS.md](AGENTS.md) DoD).

Baseline at authoring (2026-05-29): `pnpm verify` green — **143 tests / 33 files**, 0 dep violations,
`pnpm audit` clean. HEAD `029994e`.

**Cycle-2 re-baseline (2026-05-29, HEAD `dbb1fd0`):** `pnpm verify` green — **174 tests / 38 files**, 0 dep
violations, `pnpm audit --prod` clean, content = 4 packs / 14 NPCs / 3 quests / 5 locations (fingerprint
`1c8d11480b2aa5`), replay invariant holds. Cycle 1 = SPEC-01…15 **Done**, SPEC-16 carryover. Cycle 2
(SPEC-16…26, Waves 5–9) is **dependency modernization + narrative depth** — see [ROADMAP §7](ROADMAP.md#7-cycle-2-v202605--the-next-wave).

**Cycle-6 close re-baseline (2026-05-30):** `pnpm verify` green — **237 tests / 44 files**, 0 dep violations,
`pnpm audit` clean; **`pnpm e2e` 4 passed** + **production `vite build` green** (190 kB gzip). Content = 6
packs / 17 NPCs / 4 quests / 7 locations. Cycles done: 1 (SPEC-01…15), 2 (16…26), 3 (27…33), **4–6
(34…49 — the AFK arc: content reachability + all three content-safety layers test-hardened + the pure-logic
mutation sweep complete + security/docs)**. The pure-logic mutation sweep (engine-core + content-loader),
build, e2e, audit, deps, and the perf question are all resolved/verified (see [ROADMAP §9](ROADMAP.md) +
JOURNAL). **Cycle 7 (real-model generation) is human-gated** on `OPENROUTER_API_KEY` — plan in
[ROADMAP §10](ROADMAP.md); review-guide in [HANDOFF.md](HANDOFF.md).

## Status board

Status legend: `Todo` · `In progress` · `Blocked` · `Done` · `Dropped` (with reason).

| Spec | Title | Wave | Status | Branch/worktree | Commit | Verify | Notes |
|------|-------|------|--------|-----------------|--------|--------|-------|
| SPEC-01 | Doc-sync | 0 | Done | main | 67a2ea2 | green | bribe_faction + credits_at_least + v2 World fields |
| SPEC-03 | CI coverage + doctor | 0 | Done | main | 47d769d | green | @vitest/coverage-v8 + test:coverage + CI doctor/coverage; report-only; supersedes TICKET005 |
| SPEC-06 | CI supply-chain hardening | 0 | Done | main | a4a6056 | green | SHA-pinned actions + least-priv permissions + --ignore-scripts |
| SPEC-07 | depcruise layers + no-orphans | 0 | Done | main | 4f177d8 | green | 3 layer rules + no-orphans; rule fire proven via planted violation |
| SPEC-02 | Wire `talk_to` objective | 1 | Done | main | e471cc8 | green | world.dialogue signal (no new World field/migration); questSystem gains optional npcs; +1 test |
| SPEC-04 | Per-tick state-hash divergence | 1 | Done | main | 12c6f63 | green | replayTrace + firstDivergence (pure, reuse hash()); bisects divergence to a step; +1 test |
| SPEC-05 | fast-check command fuzz | 1 | Done | main | 0ffff81 | green | session-level input fuzz (60 runs, full systems path); replay-invariant-tagged; no divergence found |
| SPEC-12 | Pipeline tolerant pre-parser | 1 | Done | main | d552be1 | green | tolerantParse: fences/CoT/trailing-comma (string-aware); golden-master byte-stable; +7 tests (143→150) |
| SPEC-08 | Offline observability | 2 | Done | main | 7e99f1e | green | telemetry.ts: window error boundary + bounded ring + User Timing marks; engine-core untouched; +5 tests; browser walk PASSES (clean server) |
| SPEC-09 | Accessibility pass | 2 | Done | main | 8b5eafd | green | DialogueView: role=dialog/aria-live/focusable buttons/focus-trap+restore; reduced-motion; dyslexia-font toggle; 3 e2e a11y specs PASS (browser) |
| SPEC-10 | Durable saves + migration | 2 | Done | main | 827a741 | green | persist()/estimate/quota + migrate-on-load; **migration moved tools/migrate→engine-core** (shipped can't import tools/); +6 tests |
| SPEC-13 | New curated pack (offline) | 2 | Done | main | (see log) | green | pack.kestrel (rival fixer + 3-branch quest + Ink + canon assertions); same-path load+play test; +4 tests; golden untouched |
| SPEC-11 | Storylet / salience layer | 3 | Done | main | bb10696 | green | minimal additive schema + salience-selected ambient barks system; tie-breaker RNG inside applyEvent fold; +3 tests |
| SPEC-14 | Retrieval-grounded canon | 3 | Done | main | 816552a | green | subgraph query + cycle prompt grounding + integration spy test |
| SPEC-15 | Rubric LLM-judge gate | 3 | Done | main | abf39e2 | green | 1-5 integer rubric + rationale CoT prompt + threshold low-score flagging; Html escaping rationales; +3 tests |
| SPEC-16 | Zod 4 + native JSON Schema | 4/6 | Done | main | (see log) | green | zod 3.25→4.4.3 (root + content-schema + pipeline pkgs), zod-to-json-schema removed; native z.toJSONSchema at both sites (unrepresentable:"any"); .default({})→.prefault({}) in brief.ts; z.string().datetime()→z.iso.datetime(); Condition annotation → ZodType<C,unknown>; recursive-ref warnings GONE; golden-master byte-stable (no hash change); 174 tests |
| SPEC-17 | Doc-sync NPC schema | 5 | Done | main | (see log) | green | §3 +combat/+homeLocationId; §8 ContentPack +storylets; +Storylet subsection; docs-only, 174 tests |
| SPEC-18 | ESLint 10 + ts-eslint | 5 | Done | main | (see log) | green | eslint+@eslint/js 9.39→10, typescript-eslint 8.20→8.60; flat config unchanged (no migration); zero new lint errors; 174 tests |
| SPEC-19 | depcruise 17 + node types | 5 | Done | main | (see log) | green | dependency-cruiser 16.10→17.4.2, @types/node 22→24.12; config unchanged (v17-compatible); rule-fire re-proven via planted node:fs import (error + exit 1), reverted; 174 tests |
| SPEC-20 | TypeScript 6 | 6 | Done | main | (see log) | green | typescript 5.9.3→6.0.3; removed deprecated `baseUrl`, relativized `paths` (TS7-ready, no `ignoreDeprecations` suppression); purity re-proven (planted `document` errors under pure tsconfig.json, TS2584); ts-eslint 8.60 supports TS6 (no warning); 174 tests |
| SPEC-21 | Vitest 4 | 7 | Done | main | (see log) | green | vitest+coverage-v8 3.2.4→4.1.7 (on Vite 7.3.3); vitest.config.ts unchanged (no workspace/poolOptions to migrate); no mocks in suite (zero mock-semantics risk); replay:verify still tag-selects + green; **coverage baseline (v8 remap): 75.13% stmts / 65.14% branch / 76.14% funcs / 76.93% lines** (report-only, no floor); 174 tests |
| SPEC-22 | fast-check 4 | 7 | Done | main | (see log) | green | fast-check 3.23.2→4.8.0; no spec edits (oneof/record/constant/integer/constantFrom/array/assert/property all v4-stable); seeds still pinned (0xc0de, numRuns 60); no new divergence surfaced; 174 tests |
| SPEC-23 | `skill_at_least` condition | 8 | Done | main | (see log) | green | new content-schema/skill.ts `SkillName` (SSOT); condition + evaluator arm; effect.ts modify_skill + engine SkillId now derive from SkillName; integrity no-ref case; SCHEMA §7; +2 tests incl. SKILLS≡SkillName.options assertion (176); fingerprint + golden-master unchanged |
| SPEC-24 | Storylet pack + ambient barks | 8 | Done | main | (see log) | green | new content/core/pack.district_barks (3 fire-once ambient storylets, distinct salience, gated on flag/skill_at_least/quest); discovered storyletSystem+TriggerStorylet→ShowText already wired (SPEC-11); added thin HUD bark surfacing (main.ts captures from event stream — ShowText is a no-op on World, engine untouched); +1 spec (2 tests, 178); **e2e bark assertion PASSES in chromium**; 5 packs, fingerprint 1ffc4746 |
| SPEC-25 | content:verify storylets | 8 | Done | main | (see log) | green | new content-loader/storylet-check.ts `unsatisfiablePreconditions` (flattens `all`, catches contradictory flag_is + condition∧¬condition; documented heuristic limits) + 5 unit tests; content-verify loops storylets (error on unsat, non-fatal hygiene warning for no-precond+salience-0); refs already enforced at load (integrity.ts); planted-bad fixture proved both fire (then reverted); 183 tests |
| SPEC-26 | Pipeline emits storylets | 9 | Done | main | (see log) | green | brief budget.storylets (default 0) + StoryletProposals schema + [TASK:STORYLETS] dramatist arrow (fires only when budget>0; reactive/ambient guardrail in prompt) + synthesis includes them + Critic sees them + demo fixture emits storylet.sable_watching; **golden-master deliberately updated** (0cba→0716 default — budget.storylets enters promptHash; storylet path 0b638312); +2 cycle tests; hermetic StubProvider; verify green |
| SPEC-27 | tsconfig hygiene (+ erasableSyntaxOnly eval) | C3-P0 | Done | main | (see log) | green | vite-tsconfig-paths 5.1→6.1.1 (aliases still resolve); **erasableSyntaxOnly DEFERRED** — it bans parameter properties used in 6 DI constructors + project emits via Vite not type-stripping → net-negative (logged BACKLOG; flag proven to fire via planted enum→TS1294); @types/node kept at 24; 185 tests |
| SPEC-28 | Coverage non-regression floor | C3-P0 | Done | main | (see log) | green | vitest.config.ts thresholds 72/60/72/72 (~3pt under measured 75.71/65.82/76.87/77.62); CI `test:coverage` now enforces (comment updated); gate-fires proven (temp 99→ERROR exit 1, reverted); pnpm verify unaffected (runs `pnpm test`, not coverage) |
| SPEC-29 | tsgo typecheck accelerator | C3-P1 | Done | main | (see log) | green | @typescript/native-preview 7.0.0-dev.20260527.2 + `typecheck:fast` (tsgo, both configs); **parity proven** (tsgo zero-error on both, matches tsc); purity re-proven under tsgo (planted DOM→TS2584); `tsc` stays authoritative (`verify` unchanged); documented in plan/AGENTS.md |
| SPEC-30 | Stryker mutation testing (engine-core) | C3-P1 | Done | main | (see log) | green | Stryker 9.6.1 + vitest-runner + ts-checker; `stryker.config.json` scoped to engine-core (25 files, 1065 mutants); `pnpm mutation`; needed explicit `plugins` array (pnpm resolution). **Baseline mutation score 73.31%** (385 killed / 108 survived; 3m51s); report-only (NOT in verify). Survivor hotspots filed to BACKLOG (effects.ts 0%, snapshot 44%, combat 50%). |
| SPEC-31 | fc.commands model-based suite | C3-P1 | Done | main | (see log) | green | new app-web/test/model-based.spec.ts — fc.commands(6 cmd arbs, maxCommands 12, numRuns 40, seed-pinned) via fc.modelRun; asserts credits≥0, hp≥0, player persists, **hash(replay)===hash(live) at EVERY step**; harness catch-proven (planted break → shrunk counterexample, reverted); +2 tests (187) |
| SPEC-32 | Drama-manager waypoint salience | C3-P2 | Done | main | (see log) | green | design note in spec; pure `waypointBonus(world,storylet)` (faction-tag aligned w/ rep≥10, bounded +3, integer) → storyletSystem ranks by EFFECTIVE salience; `"main"` tag excluded; baseline-equivalent w/ no alignment; tie-break RNG unchanged (replay-safe; replay + model-based suites green); +4 tests (191). Mutation baseline predates this (storylet.ts changed). |
| SPEC-34 | Combat no-negative-HP invariant test | C4-P0 | Done | main | (see log) | green | mutation-informed; combat.test.ts overkill/post-mortem clamp test (HP≥0 every step, clamps 0, alive=hp>0); +1 test (196); purely additive. effects.ts "0%" found to be a TS-checker-error artifact, not a gap. |
| SPEC-35 | Wire Drip Market reachable | C4→C5 | Done | main | (see log) | green | UNBLOCKED via **master/plugin layering**: moved `location.drip_market` + `drip_backroom` into pack.opening (base-world *geography*); content (npcs/quest/storylets/dialogues) overlays from pack.drip_market. Added ashfall→drip_market exit + `main.ts` loads the pack. pack.opening still loads in isolation (replay-fuzz green — the earlier dependency-inversion break is resolved). +1 reachability test (199). `met_marrow` quest-offer Ink trigger → BACKLOG. |
| SPEC-36 | Multi-objective quest invariants | C5-P0 | Done | main | (see log) | green | quests.test.ts: 2-objective branch (reach→retrieve count 2) — asserts no early resolve, 1/2-done stays active (kills `every`→`some`), `>= count` threshold, ordered advance, completion. +1 test (197). quests.ts mutation survivors **20→~5** (remaining are a credits constant / reward-item edge / set_flag objective — minor, not chased per SPEC-30). |
| SPEC-37 | set_flag quest-objective test | C5-P0 | Done | main | (see log) | green | quests.test.ts: set_flag objective — unresolved while flag unset (kills `===`→`!==`), completes when set. **All 6 objective kinds now tested** (reach/retrieve/defeat/talk_to/skill_check/set_flag). +1 test (198). Full engine-core mutation re-baseline: **75.14%** (was 73.31%; killed 406/537). |
| SPEC-38 | met_marrow Ink quest trigger | C5-P2 | Done | main | (see log) | green | drip_vendor.ink sets `met_marrow` on entry; `declaredVars:["met_marrow"]`; recompiled. Dialogue system mirrors → `flag.met_marrow` → quest.market_debt now offers in-game. Completes the Drip Market playable loop (reachable + quest triggers). +1 test (200). Golden-master untouched. |
| SPEC-39 | content-loader treaty hardening | C6-P0 | Done | main | (see log) | green | REPLENISH frontier: integrity.ts (the "AI content can't break the game" safety boundary) was only 19.70% mutation-tested (npc+quest dangling only). Added load.test dangling-ref coverage for all 4 untested TYPES (faction/item/location/dialogue) + effect-routed + a table over distinct PATHS (condition/effect/objective/exit/npcSpawns/faction). +3 tests (203); the per-PATH table then extended to faction.allies/reputation_at_least/talk_to/retrieve/unlock_exit/start_quest/set_npc_dialogue/bribe_faction/reactsTo-override/storylet-dialogueId. **integrity.ts mutation 19.70%→45.45%** (killed 13→30, +17). Remaining ~34 survivors are diagnostic `where`-string details (don't affect WHETHER a ref is caught) — not chased per SPEC-30. Code unchanged (gap was coverage). |
| SPEC-40 | canon-graph semantic hardening | C6-P0 | Done | main | (see log) | green | REPLENISH frontier cont.: canon-graph.ts (the SEMANTIC contradiction detector — the other safety-critical content file) was 53.42% mutation-tested; 12 tests covered headline rules but left genuine arms: `exclusive-status` solvency group (only life tested), `npcSpawns`⟹alive (only homeLocationId tested), `serializeAssertion` enemy_of/funds/located_in, dangling-ref faction/location arms. +4 tests (207). **canon-graph.ts mutation 53.42%→58.22%** (killed 78→85). Remaining ~49 survivors are internal Map/sort plumbing + message strings (don't change detection outcome). Code unchanged. |
| SPEC-41 | Security: patch transitive `qs` | C6-P0 | Done | main | (see log) | green | REPLENISH re-audit: `pnpm audit` found 1 moderate (qs ≤6.15.1, GHSA-q8mj-m7cp-5q26) — transitive via @stryker-mutator→typed-rest-client (DEV-only; `--prod` was already clean). Pinned `qs:^6.15.2` via **pnpm-workspace.yaml `overrides`** (this repo uses pnpm-11 workspace config, not `package.json#pnpm` — first attempt there was silently ignored). qs 6.15.1→6.15.2; **`pnpm audit` now clean (rc 0)**; verify green (207). Reversible (workspace+lockfile delta only; package.json net-zero). |
| SPEC-42 | Doc-sync: pack-layering rule | C6-P0 | Done | main | (see log) | green (docs-only) | REPLENISH doc-sync: the SPEC-35 trap (loader runs on pack SUBSETS → a base pack must be self-contained; a location targeted by a cross-pack `exit` must live in the base pack as geography, content overlaid via `dependsOn`) lived only in /plan/specs, not the durable author contract. Added a "Layering corollary" para to docs/SCHEMA.md (after the integrity para) + a "Pack layering" section to content/core/README.md (was a 4-line stub), Drip Market as worked example. prettier --check clean; verify path unaffected (docs-only). |
| SPEC-43 | Playability gate: testable + tested | C6-P0 | Done | main | (see log) | green | REPLENISH frontier: the "schema-valid≠playable" gate (solvability/island-reachability/unlock_exit-bounds/offerWhen-contradiction/dead-storylet) lived INLINE in tools/scripts/content-verify.ts — exported/tested NOWHERE (the 3rd safety layer, after integrity SPEC-39 + canon SPEC-40, was the only untested one). Extracted to `@codex/content-loader` `staticPlayabilityCheck(registries)→{errors,warnings}` (behavior-preserving; script now thin CLI = load+validate+auditCanon+playability+report). +playability.test.ts: planted-violation case per rule + clean case (6 tests, 213). Real `content:verify` output identical (4 quests/7 loc/5 storylets). |
| SPEC-44 | Combat target-selection predicate tests | C6-P0 | Done | main | (see log) | green | Data-driven: an engine-core mutation RE-measure (SPEC-30 baseline stale post-34/36/37; aggregate now 69.83%) surfaced `combat.ts` STILL at 50% — SPEC-34 hardened damage/HP (applyEvent) but nothing pinned the target-SELECTION predicate (combat.ts:19 `id!==player && co-located && alive && hp!==undefined` + the Attack guard). Genuine gameplay logic (backs `defeat` + canon placement⟹alive), NOT low-risk. +5 tests (218): non-Attack ignored, dead/no-hp/cross-location/self each not targeted. **combat.ts mutation 50%→100%** (18 killed, 0 survivors). |
| SPEC-45 | rng deserialize-validation tests | C6-P0 | Done | main | (see log) | green | Same data-driven scrutiny on `rng.ts` (64.29%): sfc32 *arithmetic* survivors are correctly low-value (determinism is the tested contract; exact PRNG values brittle), but `deserializeRng`'s per-element validation (line 75 `typeof n!=="number" \|\| !Number.isFinite(n)`) — the **corrupt-save boundary** (SPEC-10 loads persisted RNG state) — was untested (only not-array + wrong-length were). Extended the malformed-state test: non-number, null, and `1e999`→Infinity elements. **rng.ts 64.29%→78.57%**; the validation cluster fully killed, residual = sfc32 arithmetic + throw-string (verified low-value). |
| SPEC-46 | snapshot hash key-order tests | C6-P0 | Done | main | (see log) | green | Final engine-core sweep file: `snapshot.ts` (43.75%). cyrb53 internals (loop/arith/padStart) correctly low-value, but `sortKeys`' **key-order normalization** (`Object.keys().sort()` + object recursion) — "the foundation of the replay invariant" (docstring) — was untested (replay.test re-runs the SAME world, never varying key order). New snapshot.test.ts: object hash independent of top-level + nested key order, sensitive to content, order-significant for arrays, round-trip. +6 tests (224). **snapshot.ts 43.75%→68.75%**; sortKeys survivors killed; residual = cyrb53 internals + 1 conditional variant (low-value, contract covered → not chased). |
| SPEC-47 | apply.ts reducer FSM + conservation edges | C6-P0 | Done | main | (see log) | green | Full mutation table (not just hotspots) showed `apply.ts` — the deterministic event-fold reducer — at 71.55% (29 survivors); I'd prematurely called the sweep done. Added apply.test cases for the highest-value cluster: ActivateQuest idempotency, CompleteQuestBranch atomicity, ForecloseBranch fail-when-empty, ResolveAttack player-force-bonus (isolated via same-rngState branching), BribeFaction afford boundary, GiveItem-exactly-zero. +6 tests (230). **apply.ts 71.55%→82.76%** (survivors 29→18). Residual genuine = TriggerStorylet selection (→ SPEC-48); rest low-value (clamp/defaults/fresh-state arrays). |
| SPEC-48 | TriggerStorylet selection tests | C6-P0 | Done | main | (see log) | green | Closed apply.ts's last genuine cluster: the `TriggerStorylet` handler (runtime salience-selected storylet delivery, SPEC-11/24). +3 apply.test cases: empty→no-op; single candidate→effect applies + rngState UNCHANGED (replay-stable fast path); multi→exactly one effect (seeded) + rngState advances. Selection made observable via per-storylet `give_item`. +3 tests (233). **apply.ts 82.76%→84.48%**; residual is the documented low-value set (clamp equivalents, objective defaults, fresh-state arrays, ambient ShowText no-op). |
| SPEC-49 | save-migration + replay-divergence robustness | C6-P0 | Done | main | (see log) | green | Final files in the pure-logic sweep. `migrate.ts` (67.86%): +2 tests — non-object/null/number input throws, non-advancing migration step throws (corrupt-save + buggy-step guards) → **100%**. `log.ts` (72.34%): +2 tests — firstDivergence length-mismatch tail (one trace longer; idempotent shared prefix isolates the tail) + replayTrace seed guard → **89.36%** (residual = version/error strings + the tick-vs-hash arm, low-value). +4 tests (237). **Pure-logic mutation sweep COMPLETE** (engine-core + content-loader). |
| SPEC-55 | Kestrel reacts to the loyalty choice | C7-P1 | Done | spec/SPEC-50-syndicate-offer | 16f818d | green | Paid off the now-live (51) rival quest's central choice with reactive dialogue on npc.kestrel: sided_with_kestrel→dialogue.kestrel_sided (warm), refused_kestrel→dialogue.kestrel_refused (cold), else default. Scoped to Kestrel's own reactsTo (NOT Varga — avoid reaction-soup; mutually-exclusive triggers = no ordering ambiguity). Reactive dialogue on a 2nd NPC (distinct from 52's Varga-reaction + 54's storylet). Recompiling kestrel left dialogue.kestrel blob byte-unchanged; 2 new dialogues referenced by reactions (SPEC-53 orphan check stays silent). rival-fixer.spec +3 (sided/refused/neither); existing 4 untouched. +3 tests (253). prod build + e2e 4 passed, golden untouched, audit clean. |
| SPEC-54 | Decrypt-path payoff storylet | C7-P1 | Done | spec/SPEC-50-syndicate-offer | 3320209 | green | Completed the Syndicate-drive choice consequence space: sell→Varga reaction (52), leverage→+8 varga rep (built-in), decrypt→**was dangling**, now a fire-once salience storylet (storylet.knows_secret, faction-tagged) surfacing once flag.knows_syndicate_secret. Storylet not NPC-reaction (no ordering precedence; ambient salience is what storylets are for). Doesn't compete w/ broker_watching (gated ¬syndicate_resolved which decrypt sets). syndicate-offer.spec +1 (GameSession firing: eligible→fires-once→replay-identical). +1 test (250). 7 storylets satisfiable, no warnings; prod build + e2e 4 passed; golden untouched, audit clean. |
| SPEC-53 | Orphaned-dialogue hygiene check | C7-P0 | Done | spec/SPEC-50-syndicate-offer | c57d9af | green | Safety-net extension (diversified from the story arc). staticPlayabilityCheck now warns on dialogues defined but referenced by NOTHING (npc.dialogueId/reactsTo-override/storylet.content.dialogueId/set_npc_dialogue effect) — the kestrel "authored-but-unreachable" class, one level down. **Warning not error** (subset loads → a dialogue referenced only by a not-yet-loaded pack would look orphaned; advisory like the salience-0 storylet warning). content:verify already prints warnings → gate surfaces it. 0 orphans today (audited) → regression guard. playability.test +2 (planted orphan→warn, errors empty; clean 4-path set→no warn). +2 tests (249). content:verify OK + 0 orphan warnings on real 7 packs; golden untouched, audit clean. |
| SPEC-52 | Varga reacts to the drive's fate (reactive payoff) | C7-P1 | Done | spec/SPEC-50-syndicate-offer | 80252d8 | green | Made the SPEC-50 "sell" choice consequential: npc.varga gains a reactsTo (appended LAST → wins over entry-method overrides; reactions apply in order, last override wins) firing on `flag.sold_drive` → new dialogue.varga_betrayed + latches flag.varga_knows_betrayal (mirrors syndicate_marked). flag is runtime state not a registry ref → pack.opening stays self-contained (replay-fuzz/isolation green); recompiling Ink left existing blobs byte-unchanged (only varga_betrayed new). reactive-payoff.spec +2 (sold-drive payoff + ordering-wins); existing 4 untouched. +2 tests (247). content fp 0233680e (content changed; pipeline golden untouched). prod build + e2e 4 passed, audit clean. |
| SPEC-51 | Wire curated kestrel pack live + live-boot-set guard | C7-P0 | Done | spec/SPEC-50-syndicate-offer | 224b59b | green | REPLENISH cont.: pack.kestrel (SPEC-13 rival fixer, curated+tested) was authored+approved but absent from main.ts's live loadPacks → unreachable by players. Wired it live (Kestrel @ ashfall_district hub, quest.rival_offer offers on flag.met_varga). Added live-packs.spec.ts: loads the EXACT live boot subset through loadPacks (content:validate only loads the superset → can't catch a subset-integrity break per SPEC-35/42) + signature entity per pack + asserts pack.bribe_demo (uncurated T-16 demo) deliberately excluded. +3 tests (245). prod build green, **e2e 4 passed** (real app boots w/ kestrel), golden untouched, audit clean. |
| SPEC-50 | Content pack: "The City's Cut" (Syndicate offer) | C7-P0 | Done | spec/SPEC-50-syndicate-offer | e3efe13 | green | REPLENISH (Cycle 7): real-model frontier is human-gated → built unblocked product depth. New pack.syndicate_offer gives faction.ashfall_syndicate an NPC face (npc.syndicate_broker @ the_drip) + Ink dialogue + 3-branch quest (sell/decrypt/leverage) gated on `flag.has_drive` (chains off quest.the_warehouse) + faction-tagged storylet + canon assertions. **Wired live** (main.ts) + same-path load+play test (offer-gating proven via the engine's ActivateQuest path; branch completes e2e w/ flag+rep consequences). Uses only existing verbs (no engine/schema change). 7 packs/18 npcs/5 quests (fp 048016af). +5 tests (242). prod build green, **e2e 4 passed**, golden untouched, audit clean. |
| SPEC-33 | New content pack (depth) | C3-P2 | Done | main | (see log) | green | content/core/pack.drip_market: 2 locations (force-gated `skill_at_least` exit), 3 NPCs + Ink (compiled), 3-branch quest (talk/muscle/sneak), reactsTo consequence, 2 fire-once storylets (one faction-tagged → SPEC-32). 6 packs/17 npcs/4 quests/7 loc (fp 1cd4c67c). +4 tests (195): same-path load, skill-gate eval, quest playthrough, storylet fire, session replay. Golden untouched. Live-app exit-wiring → BACKLOG. |

## Wave gates
- [x] **Wave 0 complete** (2026-05-29) — SPEC-01/07/03/06 shipped; `pnpm verify` green; `pnpm audit` clean. Re-baseline before Wave 1.
- [x] **Wave 1 complete** (2026-05-29) — SPEC-12/04/05/02 shipped; replay invariant intact (now also session-fuzzed); 153 tests. `pnpm verify` green. Re-baseline before Wave 2.
- [x] **Wave 2 complete** (2026-05-29) — SPEC-10/08/09/13 shipped & browser-verified; 168 tests; content = 4 packs / 14 NPCs / 3 quests; `pnpm verify` green, `pnpm audit` clean. Re-baseline before Wave 3.
  - 2026-05-29 — SPEC-10 durable saves: `requestPersistentStorage()`/`estimateStorage()` (best-effort, feature-detected) + `SaveQuotaError` handling in persistence; `loadGame` forward-migrates the snapshot World. **Spec correction:** the AC said reuse `tools/migrate`, but a shipped package can't import `tools/` (deps rule) — so the canonical runner+worldMigrations+`migrateWorld` **moved into engine-core** (pure; owns World/WORLD_VERSION) and `tools/migrate` now re-exports them (single source of truth; its test passes unchanged). app-web requests persistence on startup + toasts save failures. +6 tests (159). `pnpm verify` green. (827a741)
  - 2026-05-29 — SPEC-08 offline observability: new `app-web/src/telemetry.ts` — a top-level window `error`/`unhandledrejection` boundary records into a bounded ring (tagged with the current tick), plus no-op-safe User Timing `mark`/`measure` wrappers; `main.ts` installs the boundary, marks sim/draw phases, and exposes `__codex.telemetry()`. NO network/SaaS; engine-core untouched; `beats.ts` stays pure. +5 tests (164). `pnpm verify` green. **Browser-verified:** `pnpm e2e` first failed on `#cold-open not found` — investigated to a **foreign server squatting :4173** (`reuseExistingServer` reused it; build+serve of the real app confirmed correct, `#cold-open` present); the slice walk **PASSES** against a clean server (temp :4199 config). Filed e2e port-robustness in BACKLOG. (7e99f1e)
  - 2026-05-29 — SPEC-09 accessibility: new `app-web/src/dialogue-view.ts` makes the dialogue an ARIA modal — `role=dialog`/`aria-modal`, an `aria-live=polite` text region, real focusable `<button>` choices (rebuilt only on frame change, so the live region/focus don't churn), focus on the first choice, Tab focus-trap, and focus-restore on close; `InputController.choose()` lets buttons drive the existing Choose path (number keys still work). index.html adds the structure + `:focus-visible`, a `prefers-reduced-motion` query, readable type, and a dyslexia-friendly font toggle (`#font-toggle`, aria-pressed). engine-core untouched. 3 committed e2e a11y specs (modal contract, font toggle, **dynamic: open→focus→Esc-restores**) all PASS in chromium (run against a clean :4199 since :4173 was squatted). `pnpm verify` green; 164 unit tests. **Honest note:** structural + interactive a11y machine-verified; not audited with a real screen reader (NVDA/VoiceOver). (8b5eafd)
  - 2026-05-29 — SPEC-13 new curated pack: hand-curated `content/core/pack.kestrel` — a rival fixer (Kestrel + `faction.kestrel_outfit`, rival of Varga's crew) with a **3-branch** intro quest (take-job / play-both / stay-loyal), real Ink dialogue (compiled via `content:compile-ink`), and grounded canon assertions. `content:validate` (4 packs/14 npcs/3 quests) + `content:verify` (solvable, reachable, **canon-clean**) pass. New `rival-fixer.spec.ts` proves same-path load alongside hand-authored + generated packs, cross-pack faction refs resolve, the Ink plays, and the quest completes end-to-end through the real engine (exercising the SPEC-02 `talk_to` wiring on authored content). +4 tests (168). Golden-master untouched (no pipeline change). `pnpm verify` green. **Wave 2 complete.** Found+filed SCHEMA §3 doc-drift (combat/homeLocationId) in BACKLOG.
- [x] **Wave 3 complete** (2026-05-29) — SPEC-11/14/15 shipped (storylet layer, retrieval-grounded canon,
  rubric judge); 174 tests; `pnpm verify` green. Content depth + pipeline intelligence landed.
- [x] **Wave 4 / 6 — Zod 4 (SPEC-16)** (2026-05-29) landed in isolation; native `z.toJSONSchema`; recursive-ref warning gone; golden-master byte-stable at the time (later updated by SPEC-26's brief field).

### Cycle-2 wave gates (Waves 5–9)
- [x] **Wave 5 complete** (2026-05-29) — SPEC-17 (docs) + SPEC-19 (depcruise 17/@types/node 24) + SPEC-18 (eslint 10); `pnpm verify` green (174 tests) after each. Re-baseline before Wave 6.
- [x] **Wave 6 complete** (2026-05-29) — SPEC-16 (Zod 4 + native JSON Schema) + SPEC-20 (TS 6) shipped; recursive-ref warnings gone; engine purity re-proven; golden-master byte-stable; `pnpm verify` green (174 tests). Re-baseline before Wave 7.
- [x] **Wave 7 complete** (2026-05-29) — SPEC-21 (Vitest 4, coverage re-baselined 75.13%/65.14%/76.14%/76.93%) + SPEC-22 (fast-check 4); determinism fuzz + `replay:verify` green; 174 tests. All Cycle-2 dep modernization (Waves 5–7) landed. Re-baseline before Wave 8.
- [x] **Wave 8 complete** (2026-05-29) — SPEC-23 (`skill_at_least` passive checks) + SPEC-24 (storylet content pack + HUD barks, e2e-verified in chromium) + SPEC-25 (content:verify storylet unsatisfiability + hygiene); `pnpm verify` green (183 tests / 40 files). Re-baseline before Wave 9.
- [x] **Wave 9 complete** (2026-05-29) — SPEC-26 (pipeline emits storylets); golden-master reconciled deliberately (default 0716d252, storylet-path 0b638312); StubProvider-hermetic; `pnpm verify` green (185 tests / 40 files). **All Cycle-2 specs (SPEC-16…26) done.**

### Cycle-3 wave gates (Phases 0–2) — authored 2026-05-30, **EXECUTED 2026-05-30**
- [x] **Phase 0 complete** (2026-05-30) — SPEC-27 (vite-tsconfig-paths 6; erasableSyntaxOnly evaluated→deferred) + SPEC-28 (coverage floor 72/60/72/72, gate-fire proven); `pnpm verify` green.
- [x] **Phase 1 complete** (2026-05-30) — SPEC-29 (tsgo accelerator, parity proven; tsc authoritative) + SPEC-30 (mutation baseline **73.31%**, report-only) + SPEC-31 (fc.commands model-based suite, catch-proven); determinism gate green.
- [x] **Phase 2 complete** (2026-05-30) — SPEC-33 (Drip Market pack, same-path, end-to-end) + SPEC-32 (drama-manager waypoint salience, replay-safe); `pnpm verify` green (195 tests / 42 files). **All Cycle-3 specs (SPEC-27…33) done.**

## Changelog (append-only; newest last)
- 2026-05-29 — `/plan/` authored from the 2026 baseline + 4-agent research pass. No code changed yet.
- 2026-05-29 — SPEC-01 doc-sync: SCHEMA §5 +bribe_faction, §7 +credits_at_least, WORLD_STATE §1 +npcDialogue/+unlockedExits (v2). Docs-only; `pnpm verify` green. (67a2ea2)
- 2026-05-29 — SPEC-07 depcruise: +content-schema-is-leaf, +content-loader-only-imports-schema, +render-and-persistence-only-in-app-web, +no-orphans. Corrected mid-impl: narrative-ink is legitimately imported by offline tooling (compile-ink/synthesis) + packages import their own internals. Rule-fire proven via a planted violation. `pnpm verify` green. (4f177d8)
- 2026-05-29 — SPEC-03 CI coverage+doctor: added @vitest/coverage-v8 + `test:coverage` + vitest coverage config; CI runs `agent:doctor` before `verify` and uploads an lcov artifact. Report-only (no floor); verify stays blocking, e2e non-blocking. `pnpm verify` + `pnpm test:coverage` green. (CI run pending a push.) (47d769d)
- 2026-05-29 — SPEC-06 CI hardening: pinned all 4 GitHub Actions to commit SHAs (+ tag comments), added top-level least-privilege `permissions: contents: read`, and `--ignore-scripts` on CI installs. Proved locally `pnpm install --frozen-lockfile --ignore-scripts` succeeds (no dep needs a lifecycle script). `pnpm verify` green. **Wave 0 complete.** (YAML validity confirmable on next push.) (a4a6056)
- 2026-05-29 — SPEC-12 tolerant pre-parser: `generateStructured` now runs a string-aware `tolerantParse` (code fences / prose-wrapped JSON / trailing commas) before counting a repair, so recoverable output costs 0 re-prompts. No new dep. Golden-master byte-stable (StubProvider clean path unchanged). 150 tests (+7). `pnpm verify` green. (d552be1)
- 2026-05-29 — SPEC-04 per-tick state-hash: added pure `replayTrace`/`firstDivergence` to engine-core (reuse `hash()`), exported via the log barrel; replay.test.ts gains a divergence-bisection test (perturb an event -> pinpoints the step). Caught+fixed a `exactOptionalPropertyTypes` error mid-impl (conditional-spread the optional fields). 151 tests (+1). `pnpm verify` green. (12c6f63)
- 2026-05-29 — SPEC-05 fast-check command fuzz: new app-web/test/replay-fuzz.spec.ts drives random valid-shaped input sequences (Move/Interact/Attack/UseExit/Attempt/Bribe) through a headless GameSession and asserts hash(replay)==hash(live) — exercising the full systems->events->apply->log path (complements the applyEvent-level fuzz in replay.test.ts). 60 runs, seed-pinned; no divergence surfaced. 152 tests (+1). `pnpm verify` green. (0ffff81)
- 2026-05-29 — SPEC-02 wire talk_to: the `talk_to` objective (previously a runtime stub) now completes once the player has engaged the NPC's dialogue — detected via `world.dialogue` (set by DialogueAdvanced, replay-safe), so NO new World field/migration. `questSystem` takes an optional `npcs` registry to resolve the NPC's (base or overridden) dialogue id; session wires it in. +1 test (153). **Wave 1 complete.** `pnpm verify` green.
- 2026-05-29 — SPEC-11 storylet-layer: added Zod schema, integrity checker, deterministic salience selector system, and TriggerStorylet tie-breaker event handler inside the fold (bb10696); verify green.
- 2026-05-29 — SPEC-14 retrieval-grounded canon: queried 1-hop relevant subgraph over the S5 graph and injected as stable prompt context grounding (816552a); verify green.
- 2026-05-29 — SPEC-15 rubric-judge-gate: added locked 1-5 integer rubric, rationale-before-score prompt, advisory threshold flagging, and HTML escaping of rationales (abf39e2); verify green.
- 2026-05-29 — **Cycle 2 (v2026.05) authored** from a fresh deep audit (HEAD `dbb1fd0`, 174 tests green, audit clean) + a 2026 stack pass + a competitive/design research agent. Added SPEC-17…26 (Waves 5–9): dependency modernization (Zod 4 carryover, TS 6, Vitest 4, ESLint 10, fast-check 4, depcruise 17) + narrative depth (`skill_at_least`, storylet content+barks, content:verify storylet coverage, pipeline emits storylets). Updated ROADMAP §7, RISK_REGISTER, BACKLOG. **No code changed yet.**
- 2026-05-29 — **Cycle 2 (v2026.05) COMPLETE.** Executed SPEC-16…26 (Waves 5–9), one local commit per spec, `pnpm verify` green after each. Dependency modernization: Zod 3→4 + native `z.toJSONSchema` (SPEC-16, f13→b30fa41), TypeScript 5.9→6 (SPEC-20, c315ad5), Vitest 3→4 (SPEC-21, d3f0908), ESLint 9→10 (SPEC-18, 64e2df4), fast-check 3→4 (SPEC-22, d820c5c), dependency-cruiser 16→17 + @types/node→24 (SPEC-19, c871e98). Narrative depth: `skill_at_least` passive-check condition (SPEC-23, 5a12fa1), hand-authored storylet barks + HUD surfacing, e2e-verified in chromium (SPEC-24, 6a1b0ff), `content:verify` storylet unsatisfiability+hygiene (SPEC-25, e6655f7), offline pipeline emits reactive storylets (SPEC-26, 499c77c). Docs: SPEC-17 (f13fac7). Final: **185 tests / 40 files green, `pnpm audit --prod` clean, 5 content packs, no pushes.** golden-master deliberately updated twice (Zod4 JSON-Schema shape; budget.storylets→promptHash). HEAD 499c77c.
- 2026-05-30 — **Cycle 3 (v2026.05-c3) authored.** Post-Cycle-2 audit (HEAD `b756113`, 185 tests green, audit clean, deps essentially current) + targeted research (tsgo production-ready for noEmit; Stryker mutation testing). Dependency theme exhausted → Cycle 3 = quality-depth + features: SPEC-27 (tsconfig hygiene + erasableSyntaxOnly), SPEC-28 (coverage floor), SPEC-29 (tsgo accelerator, tsc authoritative), SPEC-30 (Stryker mutation testing on engine-core, report-only), SPEC-31 (fc.commands model-based suite), SPEC-32 (drama-manager salience, design-note-gated), SPEC-33 (new content pack). ROADMAP §8 + RISK_REGISTER + BACKLOG updated. **No code changed yet.**
- 2026-05-30 — **Cycle 3 (v2026.05-c3) COMPLETE.** Executed SPEC-27…33 (Phases 0–2), one local commit each, `pnpm verify` green after every spec. Quality-depth: SPEC-29 tsgo (TS7-native) typecheck accelerator (120045e, parity proven, tsc authoritative), SPEC-30 Stryker mutation testing on engine-core (875d93c, **baseline 73.31%**, report-only; survivors→BACKLOG), SPEC-31 fc.commands model-based determinism suite (1058b97). Hygiene/safety: SPEC-27 (a20c608, vite-tsconfig-paths 6; erasableSyntaxOnly deferred w/ rationale), SPEC-28 coverage floor (fc6fe7f). Features: SPEC-32 drama-manager waypoint salience (2acd291, pure/replay-safe), SPEC-33 Drip Market pack (0677e38, 3-branch quest + skill-gated exit + storylets). Caught+fixed a regression (eslint linting .stryker-tmp). Final: **195 tests / 42 files green, `pnpm audit --prod` clean, 6 content packs, mutation 73.31%, no pushes.** HEAD 0677e38.
<!-- Append: `YYYY-MM-DD — SPEC-NN <slug>: <what changed> (<commit>); verify <green/red>.` -->
