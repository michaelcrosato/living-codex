/**
 * Validates every content pack against the Zod schema + referential integrity (T-03).
 * Until packs and the loader exist, this is a green no-op.
 */
console.log("[content:validate] pending T-03 — no packs to validate yet.");
